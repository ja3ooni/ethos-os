# Plan 5.4 — Basic Search

**Purpose:** Search bar for finding initiatives by name, owner, or status.

**Scope:**
- GET /hierarchy/search endpoint already exists
- Search input field
- Filter by initiative level (portfolio, program, project, sprint, task)
- Filter by status/approval state

**Implementation:**
- Add search bar component
- Connect to /hierarchy/search API
- Display results grouped by type
- Include level and status filters

**Dependencies:** HIER-11 (existing)