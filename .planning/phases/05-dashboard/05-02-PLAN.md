# Plan 5.2 — Gate Status Board

**Purpose:** Read-only board showing pending gate requests with age, type, and status.

**Scope:**
- GET /gates/pending endpoint already exists
- GET /gates/dashboard provides counts
- Display columns: type, entity, age, approver, status
- Age highlighting (approaching timeout)
- Read-only (no inline approve/reject in UI)

**Implementation:**
- Add HTML template for gate board
- Fetch from /gates/pending endpoint
- Display as table with age calculation
- Show warning styling for gates approaching timeout
- Include approval rate warning (GATE theater detection)

**Dependencies:** GATE-08 (existing)