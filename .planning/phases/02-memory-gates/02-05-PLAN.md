# Plan 2.5 — Gate Dashboard Data

**Goal:** Pending gates query, approval rate, decision time tracking, API data.

**Requirements covered:** GATE-08

## Deliverables

### 2.5.1 — Dashboard Queries
- Pending gates ordered by age (oldest first)
- Gate approval rate calculation
- Average decision time by gate type

### 2.5.2 — Dashboard Service
- `get_dashboard_summary()`: Summary stats
- `get_pending_gates_with_age()`: List with age calculation
- `get_gate_metrics()`: Approval rate, avg decision time

## Implementation

- Create `ethos_os/gates/dashboard.py` with dashboard data service

## Acceptance Criteria

1. Pending gates queryable by age
2. Approval rate calculated correctly
3. Decision time tracked