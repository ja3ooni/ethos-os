# Plan 4.3 — Agent and Heartbeat API

**Goal:** Register agents, record heartbeats, retrieve timelines.

**Tasks:**
1. Already implemented in `ethos_os/api/agents.py`
2. Verify all endpoints work: register, list, get, heartbeat, timeline
3. Add missing endpoint for agent status update

**Dependencies:** Phase 3 heartbeat models, AgentRegistry

**Agent:** @frontend-developer

**Success Criteria:**
- Agent registration via POST /agents
- Heartbeat recording via POST /agents/{id}/heartbeat
- Heartbeat timeline via GET /agents/{id}/heartbeats
- Agent status queryable via GET /agents/{id}/status