# Plan 4.1 — Initiative Hierarchy API

**Goal:** CRUD endpoints for portfolio, program, project, workstream, sprint, backlog, task, subtask. Tree view, lineage, search.

**Tasks:**
1. Create `ethos_os/api/hierarchy.py` with CRUD routers for Portfolio, Program, Project, Sprint, Task
2. Add tree view endpoint returning full initiative hierarchy
3. Add lineage query per work item
4. Add search endpoints (by name, owner, status)
5. Use Pydantic for request/response validation

**Dependencies:** Phase 1 models, Phase 1 repositories

**Agent:** @frontend-developer

**Success Criteria:**
- All hierarchy CRUD operations work via REST
- Tree view returns complete initiative hierarchy
- Lineage query returns portfolio → task
- Search returns filtered results