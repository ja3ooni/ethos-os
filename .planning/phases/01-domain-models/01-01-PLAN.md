---
phase: 01-domain-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, .env.example, ethos_os/__init__.py, ethos_os/config.py, ethos_os/db.py, ethos_os/models/__init__.py, ethos_os/repositories/__init__.py]
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03]

must_haves:
  truths:
    - "Project builds with 'pip install -e .' and all dependencies resolve"
    - "Configuration loads from both YAML files and environment variables"
    - "SQLAlchemy session factory creates and manages connections"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata, dependencies, uv/poetry config"
    - path: ".env.example"
      provides: "Environment variable template for local development"
    - path: "ethos_os/config.py"
      provides: "Configuration loader (YAML + env vars)"
    - path: "ethos_os/db.py"
      provides: "SQLAlchemy session factory, Base, engine creation"
  key_links:
    - from: "ethos_os/config.py"
      to: "ethos_os/db.py"
      via: "get_database_url() called by create_engine()"
---

<objective>
Project scaffolding with Python project setup, configuration management, and database connection infrastructure.

Purpose: Establish the foundation all subsequent phases build on. No domain code yet — just the plumbing.

Output: Runnable project with `pip install -e .` working, config loading, database connection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-models/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task: Set up Python project with pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Create pyproject.toml with:
- name = "ethos-os"
- version = "0.1.0"
- description = "Initiative-based OS for human-agent organizations"
- requires-python = ">=3.11"
- dependencies: fastapi, sqlalchemy>=2.0, alembic, pydantic, pydantic-settings, pyyaml, qdrant-client, python-dotenv
- build-system with uv or poetry
  </action>
  <verify>
<automated>python -c "import ethos_os; print('ethos_os imports')"</automated>
  </verify>
  <done>pyproject.toml creates installable package with `pip install -e .`</done>
</task>

<task type="auto">
  <name>Task: Create config loader</name>
  <files>ethos_os/config.py, .env.example</files>
  <action>
Create ethos_os/config.py with:
- Config class using pydantic-settings
- Settings: database_url (SQLite path), qdrant_url, qdrant_api_key, heartbeat_interval (default: 30), debug (default: false)
- Load order: env vars override YAML config
- Use pydantic_settings.SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")
- Include YAML config loading: _load_yaml_config() reads ethos.yaml if exists
  </action>
  <verify>
<automated>python -c "from ethos_os.config import get_settings; s = get_settings(); print(f'db: {s.database_url}')"</automated>
  </verify>
  <done>Config loads from .env or defaults; all settings accessible via get_settings()</done>
</task>

<task type="auto">
  <name>Task: Set up SQLAlchemy infrastructure</name>
  <files>ethos_os/db.py</files>
  <action>
Create ethos_os/db.py with:
- SQLAlchemy 2.x style imports (DeclarativeBase)
- create_engine() with database_url from config
- get_session() -> generator yielding Session
- Session commit on success, rollback on exception
- Include Base for all models to inherit
- Add engine dispose on shutdown
  </action>
  <verify>
<automated>python -c "from ethos_os.db import get_session; print('session factory works')"</automated>
  </verify>
  <done>Session factory creates valid SQLAlchemy sessions; engine dispose works</done>
</task>

<task type="auto">
  <name>Task: Create package init and directory structure</name>
  <files>ethos_os/__init__.py, ethos_os/models/__init__.py, ethos_os/repositories/__init__.py, ethos_os/memory/__init__.py</files>
  <action>
Create:
- ethos_os/__init__.py: __version__ = "0.1.0"
- ethos_os/models/__init__.py: placeholder for domain models in Plans 02+
- ethos_os/repositories/__init__.py: placeholder for repos in Plans 03+
- ethos_os/memory/__init__.py: placeholder for working memory in Plan 04
- Also create ethos_os/gates/, ethos_os/api/, ethos_os/execution/ as empty __init__.py for completeness
  </action>
  <verify>
<automated>python -c "import ethos_os; print(ethos_os.__version__)"</automated>
  </verify>
  <done>Package imports without errors; all submodules resolve</done>
</task>

</tasks>

<verification>
- [ ] `pip install -e .` succeeds without errors
- [ ] `python -c "from ethos_os.config import get_settings; print(get_settings().database_url)"` outputs SQLite path
- [ ] `python -c "from ethos_os.db import get_session; s = next(get_session()); print('connected')"` connects to SQLite
- [ ] `python -c "import ethos_os; print(ethos_os.__version__)"` outputs "0.1.0"
</verification>

<success_criteria>
- pyproject.toml with all required dependencies
- .env.example with DATABASE_URL, QDRANT_URL, QDRANT_API_KEY template
- Config loader supporting YAML + env vars
- SQLAlchemy session factory operational
- Package installs and imports cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-models/01-01-SUMMARY.md`
</output>