---
phase: 01-domain-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [ethos_os/models/portfolio.py, ethos_os/models/program.py, ethos_os/models/project.py, ethos_os/models/sprint.py, ethos_os/models/task.py, ethos_os/models/prd.py]
autonomous: true
requirements: [HIER-01, HIER-02, HIER-03, HIER-04, HIER-05, HIER-06, HIER-07, HIER-08, HIER-09, HIER-10, HIER-11, HIER-12]

must_haves:
  truths:
    - "All hierarchy entities have UUID, name, created_at, updated_at, owner_id"
    - "Materialized path enables O(1) lineage query without recursive CTE"
    - "Project requires board-approved PRD before sprints can spawn"
    - "Task creation validates prd_scope_item_id is present"
  artifacts:
    - path: "ethos_os/models/portfolio.py"
      provides: "Portfolio model with path_depth=1"
    - path: "ethos_os/models/program.py"
      provides: "Program model with path_depth=2, FK to portfolio"
    - path: "ethos_os/models/project.py"
      provides: "Project model with path_depth=3, FK to program, prd_status enum"
    - path: "ethos_os/models/sprint.py"
      provides: "Sprint model with path_depth=4, FK to project, validates prd_status=approved"
    - path: "ethos_os/models/task.py"
      provides: "Task model with path_depth=5, validates prd_scope_item_id required"
    - path: "ethos_os/models/prd.py"
      provides: "PRD model with metadata fields (intent, success_metric, scope, boundaries, etc.)"
  key_links:
    - from: "ethos_os/models/program.py"
      to: "ethos_os/models/portfolio.py"
      via: "portfolio_id foreign key"
    - from: "ethos_os/models/project.py"
      to: "ethos_os/models/program.py"
      via: "program_id foreign key"
    - from: "ethos_os/models/sprint.py"
      to: "ethos_os/models/project.py"
      via: "project_id foreign key, validates project.prd_status == approved"
    - from: "ethos_os/models/task.py"
      to: "ethos_os/models/sprint.py"
      via: "sprint_id foreign key, prd_scope_item_id required"
---

<objective>
Initiative hierarchy models implementing portfolio → program → project → sprint → task structure with materialized path for O(1) lineage queries.

Purpose: Core domain models — portfolio/ program/project with board-reviewed PRD (intent, success_metric, scope, boundaries, timeline, budget), sprints with capacity, tasks with PRD reference enforcement.

Output: SQLAlchemy models inheriting from Base, path_depth field for level filtering, materialized path format `/uuid/uuid/...`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-models/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/ONTOLOGY.md
@docs/ARCHITECTURE.md

<!-- Key decisions from CONTEXT.md to implement exactly: -->
<!-- D-04: Materialized path with parent_id + path -->
<!-- D-05: Path format "/portfolio_id/program_id/project_id/sprint_id/task_id" -->
<!-- D-06: O(1) lineage via path split -->
<!-- D-07: Path migration for descendants on parent change -->
<!-- D-08: path_depth field for level filtering -->
<!-- D-18: Entities: Portfolio, Program, Project, PRD, Sprint, Task -->
<!-- D-20: Project prd_status: draft → pending_approval → approved → archived -->
<!-- D-21: Sprint goal, start_date, end_date, capacity_hours -->
<!-- D-22: Task prd_scope_item_id required -->
<!-- D-44: Task creation validates prd_scope_item_id is not null -->
<!-- D-45: Sprint creation validates project.prd_status == approved -->
</context>

<tasks>

<task type="auto">
  <name>Task: Create base model and mixins</name>
  <files>ethos_os/models/base.py</files>
  <action>
Create ethos_os/models/base.py with:
- TimestampMixin: created_at, updated_at with server_default=func.now()
- UUIDMixin: id (UUID, primary_key=True, default=uuid4)
- OwnerMixin: owner_id (UUID, nullable for system-created)
- PathMixin: 
  - parent_id (UUID, nullable for root entities)
  - path (String, stored materialized path like "/uuid/uuid/")
  - path_depth (Integer, 1=portfolio, 2=program, 3=project, 4=sprint, 5=task)
- All mixins using SQLAlchemy 2.x style ( declarative_mapper_arguments )
  </action>
  <verify>
<automated>python -c "from ethos_os.models.base import UUIDMixin, PathMixin; print('mixins import')"</automated>
  </verify>
  <done>Base mixins created; UUIDs generate on insert; path fields available</done>
</task>

<task type="auto">
  <name>Task: Create hierarchy entity models</name>
  <files>ethos_os/models/portfolio.py, ethos_os/models/program.py, ethos_os/models/project.py</files>
  <action>
Create each model inheriting from Base + mixins:

Portfolio (path_depth=1):
- name (String, required)
- strategic_intent (Text, optional — why this portfolio exists)
- No parent (root entity)

Program (path_depth=2):
- name (required)
- portfolio_id (FK to Portfolio, required)
- description (Text, optional)

Project (path_depth=3):
- name (required)
- program_id (FK to Program, required)
- prd_status: Enum('draft', 'pending_approval', 'approved', 'archived')
- intent (Text) — one-line intent
- success_metric (Text) — measurable success criteria
- scope (Text) — what's in/out
- boundaries (Text) — explicit exclusions
- timeline (JSON) — {start, end, milestones}
- budget (Numeric) — allocated budget cents
- qdrant_collection_name (String, nullable)
- qdrant_point_id (String, nullable)
- validation: prd_status == 'approved' required for Sprint creation (D-45)
  </action>
  <verify>
<automated>python -c "from ethos_os.db import Base; from ethos_os.models.portfolio import Portfolio; from ethos_os.models.program import Program; from ethos_os.models.project import Project; print('models import')"</automated>
  </verify>
  <done>Portfolio, Program, Project models create tables with correct relationships</done>
</task>

<task type="auto">
  <name>Task: Create task and sprint models</name>
  <files>ethos_os/models/sprint.py, ethos_os/models/task.py</files>
  <action>
Create each model:

Sprint (path_depth=4):
- name (required)
- project_id (FK to Project, required)
- goal (Text) — sprint objective
- start_date (Date)
- end_date (Date)
- capacity_hours (Integer) — available hours
- status: Enum('planning', 'active', 'completed', 'cancelled')
- validation: project.prd_status must be 'approved' (D-45)

Task (path_depth=5):
- name (required)
- sprint_id (FK to Sprint, required)
- prd_scope_item_id (UUID, required) — reference to PRD scope section (D-44)
- effort_estimate_hours (Numeric, nullable)
- assignee_id (UUID, nullable)
- status: Enum('todo', 'in_progress', 'done', 'blocked')
- description (Text)
- validation: prd_scope_item_id is not null (D-44)
  </action>
  <verify>
<automated>python -c "from ethos_os.models.sprint import Sprint; from ethos_os.models.task import Task; print('sprint/task import')"</automated>
  </verify>
  <done>Sprint validates project approval; Task validates PRD scope reference</done>
</task>

<task type="auto">
  <name>Task: Create PRD model</name>
  <files>ethos_os/models/prd.py</files>
  <action>
Create ethos_os/models/prd.py:

PRD model:
- project_id (FK to Project, required)
- intent (Text, required) — one-line intent
- success_metric (Text, required) — measurable success
- scope (Text) — what's included
- boundaries (Text) — explicit exclusions
- timeline (JSON) — {start, end, milestones: [{name, date}]}
- budget (Numeric) — budget in cents
- status: Enum('draft', 'pending_review', 'approved', 'rejected', 'archived')
- qdrant_collection_name (String, nullable)
- qdrant_point_id (String, nullable)
- created_by (UUID)
- reviewed_by (UUID, nullable)
- reviewed_at (DateTime, nullable)
- One PRD per Project
  </action>
  <verify>
<automated>python -c "from ethos_os.models.prd import PRD; print('prd imports')"</automated>
  </verify>
  <done>PRD model with full metadata; status workflow from draft → approved</done>
</task>

<task type="auto">
  <name>Task: Create models __init__ with exports</name>
  <files>ethos_os/models/__init__.py</files>
  <action>
Update ethos_os/models/__init__.py to export:
- from ethos_os.models.base import Base, UUIDMixin, TimestampMixin, OwnerMixin, PathMixin
- from ethos_os.models.portfolio import Portfolio
- from ethos_os.models.program import Program
- from ethos_os.models.project import Project, ProjectStatus
- from ethos_os.models.sprint import Sprint, SprintStatus
- from ethos_os.models.task import Task, TaskStatus
- from ethos_os.models.prd import PRD, PRDStatus

Add __all__ list with all exported names.
  </action>
  <verify>
<automated>python -c "from ethos_os.models import Portfolio, Program, Project, Sprint, Task, PRD; print('all models export')"</automated>
  </verify>
  <done>All models importable from ethos_os.models;__all__ defined</done>
</task>

</tasks>

<verification>
- [ ] All entities have id (UUID), created_at, updated_at, owner_id
- [ ] Materialized path stored in path field
- [ ] path_depth enables level filtering
- [ ] Sprint creation fails if project.prd_status != approved
- [ ] Task creation fails if prd_scope_item_id is null
- [ ] Lineage can be computed from path via O(1) split
</verification>

<success_criteria>
- Portfolio → Program → Project → Sprint → Task hierarchy complete
- Materialized path format: /uuid/uuid/uuid/uuid/uuid
- path_depth: 1-5 mapping to entity levels
- PRD reference validation enforced at model level (not just API)
- Project status workflow: draft → pending_approval → approved
- Sprint capacity tracking with dates
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-models/01-02-SUMMARY.md`
</output>