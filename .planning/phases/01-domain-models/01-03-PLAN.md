---
phase: 01-domain-models
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [ethos_os/repositories/portfolio.py, ethos_os/repositories/program.py, ethos_os/repositories/project.py, ethos_os/repositories/sprint.py, ethos_os/repositories/task.py, ethos_os/repositories/prd.py, ethos_os/repositories/base.py]
autonomous: true
requirements: [HIER-01, HIER-02, HIER-03, HIER-04, HIER-05, HIER-06, HIER-07, HIER-08, HIER-09, HIER-10, HIER-11, HIER-12]

must_haves:
  truths:
    - "CRUD operations for all hierarchy entities via repository pattern"
    - "Lineage query O(1) via materialized path split"
    - "Tree query returns full portfolio → task lineage"
    - "Search by name, owner, status works"
  artifacts:
    - path: "ethos_os/repositories/base.py"
      provides: "BaseRepository with SQLAlchemy session management"
    - path: "ethos_os/repositories/portfolio.py"
      provides: "PortfolioRepository with create, get, list, update, delete"
    - path: "ethos_os/repositories/program.py"
      provides: "ProgramRepository with list_by_portfolio"
    - path: "ethos_os/repositories/project.py"
      provides: "ProjectRepository with list_by_program, get_with_lineage"
    - path: "ethos_os/repositories/sprint.py"
      provides: "SprintRepository with list_by_project, get_lineage (O(1))"
    - path: "ethos_os/repositories/task.py"
      provides: "TaskRepository with list_by_sprint, update_path_on_move"
    - path: "ethos_os/repositories/prd.py"
      provides: "PRDRepository with create, get, approve"
  key_links:
    - from: "ethos_os/repositories/base.py"
      to: "ethos_os/db.py"
      via: "get_session() passed to BaseRepository.__init__"
    - from: "ethos_os/repositories/*"
      to: "ethos_os/models/*"
      via: "import models, query using model classes"
    - from: "task.py"
      to: "sprint.py"
      via: "task_repository.list_by_sprint() calls sprint lineage"
---

<objective>
SQLAlchemy repositories implementing CRUD for all hierarchy entities with the repository pattern. Lineage and tree queries for portfolio → subtask traversal.

Purpose: Domain layer talks to repositories via interfaces (D-11), not SQLAlchemy directly. All CRUD with validation, lineage queries.

Output: Repository instances with create/get/list/update/delete methods, lineage queries, search.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-models/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/ONTOLOGY.md

<!-- Key context: -->
<!-- D-11: Repository pattern — domain layer talks to repositories, not SQLAlchemy directly -->
<!-- D-06: Lineage query (sprint → portfolio) is O(1) — split path and resolve segments -->
<!-- D-07: Moving entity to new parent updates path for all descendants -->
</context>

<tasks>

<task type="auto">
  <name>Task: Create base repository</name>
  <files>ethos_os/repositories/base.py</files>
  <action>
Create ethos_os/repositories/base.py with:
- BaseRepository class with:
  - __init__(self, session: Session)
  - _commit() — commits and refreshes
  - _rollback() — rolls back on exception
  - _get_or_404(id, model) — raises HTTP 404 if not found
- Generic methods:
  - create(data: dict) -> model instance
  - get(id) -> model | None
  - list(filters: dict = None, order_by = None, limit = None) -> list
  - update(id, data) -> model
  - delete(id) -> bool
  - exists(id) -> bool
  - count(filters: dict = None) -> int
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.base import BaseRepository; print('base repo works')"</automated>
  </verify>
  <done>BaseRepository provides CRUD scaffolding for all entities</done>
</task>

<task type="auto">
  <name>Task: Create Portfolio and Program repositories</name>
  <files>ethos_os/repositories/portfolio.py, ethos_os/repositories/program.py</files>
  <action>
Create repos:
- PortfolioRepository(BaseRepository):
  - get_by_name(name) -> Portfolio | None
  - list_by_owner(owner_id) -> list
  - create sets path_depth=1, path="/uuid/" format

- ProgramRepository(BaseRepository):
  - list_by_portfolio(portfolio_id) -> list
  - get_lineage(program_id) -> list[Portfolio] O(1) via path split
  - create: validates portfolio_id exists, sets path_depth=2, updates path
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.portfolio import PortfolioRepository; from ethos_os.repositories.program import ProgramRepository; print('portfolio/program repos')"</automated>
  </verify>
  <done>Portfolio CRUD; Program CRUD + lineage via path split</done>
</task>

<task type="auto">
  <name>Task: Create Project repository</name>
  <files>ethos_os/repositories/project.py</files>
  <action>
Create ProjectRepository(BaseRepository):
- list_by_program(program_id) -> list
- list_by_status(status) -> list
- search_by_name(query: str) -> list (LIKE search)
- get_with_lineage(project_id) -> {project, program, portfolio} dict
- create: validates program_id, sets path_depth=3
- update_path(entity, new_parent_id): updates entity.path + all descendant paths in one transaction (D-07)
- approve(prd_id): sets prd_status = 'approved', sets reviewed_* fields
- list_approved(): projects with prd_status == 'approved'
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.project import ProjectRepository; print('project repo')"</automated>
  </verify>
  <done>Project CRUD; search; lineage; approval workflow</done>
</task>

<task type="auto">
  <name>Task: Create Sprint repository</name>
  <files>ethos_os/repositories/sprint.py</files>
  <action>
Create SprintRepository(BaseRepository):
- list_by_project(project_id) -> list
- list_active() -> list
- get_lineage(sprint_id) -> dict with {sprint, project, program, portfolio}
- create: validates project.prd_status == 'approved' (D-45), sets path_depth=4
- list_by_date_range(start, end) -> list
- get_capacity_used(project_id) -> int (sum of task hours in active sprints)
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.sprint import SprintRepository; print('sprint repo')"</automated>
  </verify>
  <done>Sprint CRUD; validates project approval at create time; capacity tracking</done>
</task>

<task type="auto">
  <name>Task: Create Task repository</name>
  <files>ethos_os/repositories/task.py</files>
  <action>
Create TaskRepository(BaseRepository):
- list_by_sprint(sprint_id) -> list
- list_by_assignee(assignee_id) -> list
- list_by_status(status) -> list
- get_lineage(task_id) -> dict with full lineage
- create: validates prd_scope_item_id is not null (D-44)
- move_to_sprint(task_id, new_sprint_id): updates task + all subtask paths (D-07)
- get_by_prd_scope(prd_scope_item_id) -> list
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.task import TaskRepository; print('task repo')"</automated>
  </verify>
  <done>Task CRUD; validates PRD scope at create; move with path migration</done>
</task>

<task type="auto">
  <name>Task: Create PRD repository</name>
  <files>ethos_os/repositories/prd.py</files>
  <action>
Create PRDRepository(BaseRepository):
- list_by_project(project_id) -> list
- get_latest_by_project(project_id) -> PRD | None
- search_by_intent(query: str) -> list
- create: sets status = 'draft'
- approve(prd_id, reviewer_id): sets status = 'approved', reviewed_by, reviewed_at
- reject(prd_id, reviewer_id, notes): sets status = 'rejected'
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories.prd import PRDRepository; print('prd repo')"</automated>
  </verify>
  <done>PRD CRUD + approve/reject workflow</done>
</task>

<task type="auto">
  <name>Task: Create repositories __init__</name>
  <files>ethos_os/repositories/__init__.py</files>
  <action>
Update ethos_os/repositories/__init__.py:
- from ethos_os.repositories.base import BaseRepository
- from ethos_os.repositories.portfolio import PortfolioRepository
- from ethos_os.repositories.program import ProgramRepository
- from ethos_os.repositories.project import ProjectRepository
- from ethos_os.repositories.sprint import SprintRepository
- from ethos_os.repositories.task import TaskRepository
- from ethos_os.repositories.prd import PRDRepository
- __all__ list
  </action>
  <verify>
<automated>python -c "from ethos_os.repositories import PortfolioRepository, ProjectRepository, TaskRepository; print('repos export')"</automated>
  </verify>
  <done>All repositories export from package</done>
</task>

</tasks>

<verification>
- [ ] Portfolio → Task traversal via path split returns complete lineage
- [ ] Task creation fails without prd_scope_item_id (validation error)
- [ ] Sprint creation fails without approved project (validation error)
- [ ] Search by name returns matching entities
- [ ] Path migration updates all descendants atomically
</verification>

<success_criteria>
- Repository pattern with SQLAlchemy session isolation
- O(1) lineage via path split (no recursive CTE)
- CRUD operations for all hierarchy entities
- Validation at repository layer (not just model)
- Search by name, owner, status
- Path migration for entity moves
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-models/01-03-SUMMARY.md`
</output>